import React from 'react';
import Layout from '../../components/Layout';

const Home = () => {
    return (
        <Layout>
            <div>index</div>
        </Layout>
    );
};

export default Home;
